extends .layout

block content
	h1(translate) made with

	{{ with .CurrentTech }}
	a.made-with__item(href="/using/{{ .URLName }}")
		img(src='{{ asset (printf "logos/%s.svg" .URLName) }}')
		span(translate) {{ .DisplayName }}
	{{ end }}

	{{ .Works | madeWith .CurrentTech | intoGallery (printf "tech-%s" .CurrentTech.URLName) | tindent 1 }}
